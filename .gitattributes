# LF line endings in the working tree on every platform (Windows included), so tests that read files see the same bytes everywhere
* text=auto eol=lf

# Binary assets: no line ending conversion, no text diff
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary
*.vsix binary
